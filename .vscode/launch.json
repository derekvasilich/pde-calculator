{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Clang++ Debug via Make",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/pde-core",
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "Make Build"
        }
    ]
}